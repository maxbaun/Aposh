local:
  vhost: "aposh.dev"
  wordpress_path: "/srv/www/aposh" # use an absolute path here

  database:
    name: "aposh"
    user: "wp"
    password: "wp"
    host: "localhost"

  paths:
    wp_content: "content"
    uploads: "content/uploads"
    plugins: "content/plugins"
    themes: "content/themes"
    languages: "content/languages"

production:
  vhost: "aposhproduction.com"
  wordpress_path: "/home/tagprints/public_html/aposhproduction" # use an absolute path here

  paths:
    wp_content: "content"
    uploads: "content/uploads"
    plugins: "content/plugins"
    themes: "content/themes"
    languages: "content/languages"  

  database:
    name: "database_name"
    user: "user"
    password: "password"
    host: "host"
    # port: "3308" # Use just in case you have exotic server config
    # mysqldump_options: "--max_allowed_packet=50MB" # Only available if using SSH

  exclude:
    - ".git/"
    - ".gitignore"
    - ".sass-cache/"
    - "bin/"
    - "tmp/*"
    - "Gemfile*"
    - "Movefile"
    - "wp-config.php"
    - "wp-content/*.sql"

  ssh:
    host: "tagprints.com"
    user: "tagprints"
